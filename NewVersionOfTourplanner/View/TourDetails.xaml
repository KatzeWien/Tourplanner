<Window x:Class="NewVersionOfTourplanner.View.TourDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewVersionOfTourplanner.View"
        mc:Ignorable="d"
        Title="TourDetails" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding Path=Name}"></TextBox>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Path=Description}"></TextBox>
        <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding Path=From}"></TextBox>
        <TextBox Grid.Row="3" Grid.Column="0" Text="{Binding Path=To}"></TextBox>
        <TextBox Grid.Row="4" Grid.Column="0" Text="{Binding Path=TransportType}"></TextBox>
        <TextBox Grid.Row="5" Grid.Column="0" Text="{Binding Path=TourDistance}"></TextBox>
        <TextBox Grid.Row="6" Grid.Column="0" Text="{Binding Path=EstimatedTime}"></TextBox>
        <Button Grid.Row ="7" Click="ButtonDeleteTour_Click">Delete this tour</Button>
    </Grid>
</Window>
